general readme

start an empty container with "sudo docker build -t 'name' ."
requires dockerfile with at least an OS imported. 
example: FROM debian:bullseye

enter this abomination with sudo docker run -it 'name' sh

from here run commands to setup the setup
